version: "3.9"

networks:
    backend:
        external: true

services:
  mongodb:
    image: mongo
    ports: 
    - 27017:27017
  
  mongo_seed:
    image: mongo
    links:
      - mongodb
    volumes:
      - ./mongo-seed:/mongo-seed
    command:
      /mongo-seed/import.sh